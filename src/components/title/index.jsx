

export default function Title({title}){
    return(
        <div>
            <h2 className="text-raisinBlack text-[42px] font-bold leading-[50px] tracking-tight-custom">{title}</h2>
        </div>
    )
}